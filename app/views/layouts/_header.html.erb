<header>
  <div>
  <% if user_signed_in? %>
    <nav>
      <ul>
        <li><%= link_to '<', root_path %></li>
        <li><%= yield(:title) %></li>
        <li><%= link_to 'Log out', destroy_user_session_path, method: :delete%></li>
      </ul>
    </nav>
		<%end%>
  </div>
</header>